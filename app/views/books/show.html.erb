    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<div class="container">
  <div class="row">
    <div class="col-lg-4">
    <section>
      <h1>User info</h1>
      <%= attachment_image_tag @book2.user, :profile_image, :fill, 50, 50 %>

      <p>name : <%= @book2.user.name %></p>

      <p>introduction : <%= @book2.user.introduction %></p>

      <% if @book2.user.id == current_user.id %>
      <p><%= link_to "プロフィール編集", edit_user_path(@book2.user), class: "pull-leftt" %></p>
      <% end %>

    </section>

    <h3>New book</h3>
    <!-- form_for部分 -->
    <%= form_for(@book) do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Body</h4>
    <%= f.text_area :body %>

    <br><%= f.submit 'Create Book' %></br>

    <% end %>
  </div>


    <div class="col-lg-8">
      <table class="table table-striped">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinon</th>
            <% if @book2.user.id == current_user.id %>
            <th></th>
            <th></th>
            <% end %>
        
            

          
          </thead>

        <tbody>
            <tr> 
              <th>
                <%= attachment_image_tag @book2.user, :profile_image, :fill, 50, 50 %>
              
                <%= link_to @user.name, user_path(@user)  %>
              </th>
              <td>
                  <%= @book2.title %>
              </td>
              <td>
                  <%= @book2.body %>
              </td>
              <% if @book2.user.id == current_user.id %>
                   <td>
                     <%= link_to "Edit", edit_book_path(@book2.id) %>
                   </td>
                   <td>
                     <%= link_to "delete", book_path(@book2.id), method: :delete, "data-confirm" => "本当に消しますか？" %>
                   </td>
              <% end %>
              

            </tr>  
          
        </tbody>   
      </table>
    </div>
  </div>
</div>